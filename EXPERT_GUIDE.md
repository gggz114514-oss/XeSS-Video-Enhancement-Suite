# R4 参数说明

本次发布不再保留 R3 专家节点。新节点按后端配置内部参数，用户只需选择算法、倍率、编码器和增强开关。

- 默认：GPU Block、1.5×、自动编码、增强关闭。
- 脸部有竖纹：打开抗竖纹，先用短片确认细节是否符合预期。
- 静态素材：可尝试五帧融合；明显运动场景不建议默认启用。
- 超分并插帧：优先使用组合节点，在最后只锐化一次。
- 保存中间成片：FFV1 + MKV，注意空间。
- 算法效果不合适：明确选择 CPU DIS 对比；GPU DIS、AMD 光流仍是实验选择。

完整支持表和输入限制见 [README](README.md)。源码里的内部 CLI 仅用于开发与诊断，不构成旧专家节点的兼容承诺。
